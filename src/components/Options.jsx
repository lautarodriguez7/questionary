import React from 'react'

const options = (props) => {
    return (
        <div className='options'>
             <input type="radio"/> {props.options}
        </div>
    )
}

export default options
